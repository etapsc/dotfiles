# BRIDGE Project Configuration for Codex
# Place this in .codex/config.toml at your project root

# Allow AGENTS.md to provide full project context.
project_doc_max_bytes = 65536

# Codex CLI 0.130.0+ ignores project-local profile/profile blocks.
# Put optional profiles in ~/.codex/config.toml instead.
