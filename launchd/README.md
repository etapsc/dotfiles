# Zellij web access - Sleipnir launchd files

Reference copies, installed manually (not stowed):
- dev.zellij.web.plist, dev.lego.renew.plist -> ~/Library/LaunchAgents/ (launchctl bootstrap gui/501 <plist>)
- lego-renew.sh -> ~/.local/bin/ (chmod 755; expects CF token in ~/.config/lego/env, chmod 600, never committed)

Runbook: 'Zellij Web Access' note in MySecondBrain inbox.
